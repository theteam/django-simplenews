from setuptools import setup, find_packages

setup(
    name = 'simplenews',
    version = '0.1',
    url = '',
    license = '',
    description = '',
    author = '',
    packages = find_packages('src'),
    package_dir = {'': 'src'},
    install_requires = ['setuptools'],
)
